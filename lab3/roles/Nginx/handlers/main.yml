---
# handlers file for Nginx
- name: start nginx service 
  systemd:
    name: nginx 
    state: started
    enabled: yes
- name: restart nginx service 
  systemd:
    name: nginx 
    state: restarted
  